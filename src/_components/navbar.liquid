<div class="lg:hidden">
  {% if page.url == "/" %}
    <img src="/images/homepage-harry-potter.jpg"/>
  {% endif %}
  {% render "mobilemenu", page: page %}
</div>

<div class="hidden lg:block">
  {% render "desktopmenu", page: page %}
</div>

<nav {% if page.url == "/" %} id="myHeader" {% endif %} class="{% if page.url != "/"%} fixed top-0 w-full {% endif %} header hidden z-20 lg:block bg-honeycomb-dark border-b-2 border-primary text-secondary grid grid-cols-3 gap-4">
  <section class="max-w-7xl mx-auto col-start-2 col-end-3 z-40 flex space-x-4 pt-6 pb-8 justify-center items-center uppercase">
    <a href="/about" class="hover:text-light">About</a>
    <a href="/posts" class="hover:text-light">Posts</a>
  </section>
</nav>

